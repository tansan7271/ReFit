import { PlaceholderScreen } from '@/components/ui/PlaceholderScreen';

/** TODO: 회원가입 화면 구현 (이메일/비밀번호/닉네임 → services/api.ts register) */
export default function RegisterScreen() {
  return (
    <PlaceholderScreen title="회원가입" note="구조만 잡힘 — 이후 구현 예정" />
  );
}
