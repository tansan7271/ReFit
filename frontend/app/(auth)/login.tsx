import { PlaceholderScreen } from '@/components/ui/PlaceholderScreen';

/** TODO: 로그인 화면 구현 (이메일/비밀번호 → services/api.ts login) */
export default function LoginScreen() {
  return <PlaceholderScreen title="로그인" note="구조만 잡힘 — 이후 구현 예정" />;
}
